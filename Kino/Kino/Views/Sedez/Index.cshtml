@model IEnumerable<Kino.Models.Sedez>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.IdDvorane)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Vrsta)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Stevilka)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.IdDvorane)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Vrsta)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Stevilka)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdSedeza">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdSedeza">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdSedeza">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
